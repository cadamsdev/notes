{
  "name": "notex",
  "version": "0.5.0",
  "private": true,
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "bun run --filter desktop tauri build",
    "dev": "cross-env TAURI_ENV_USE_TEST_DB=true bun run --filter desktop tauri dev",
    "prod": "bun run --filter desktop tauri dev",
    "lazy-release": "lazy-release"
  },
  "workspaces": [
    "desktop"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@lazy-release/cli": "^0.0.2-snapshot.0",
    "@types/better-sqlite3": "^7.6.13",
    "concurrently": "^9.2.1",
    "cross-env": "^10.1.0",
    "wait-on": "^8.0.4"
  }
}
