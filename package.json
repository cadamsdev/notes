{
  "name": "notes",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"bun run --filter api dev\" \"wait-on http://localhost:3001/health-check && bun run --filter web dev\"",
    "web:dev": "bun run --filter web dev",
    "api:dev": "bun run --filter api dev",
    "build:web": "bun run --filter web build",
    "build:api": "bun run --filter api build",
    "desktop": "bun run --filter desktop start"
  },
  "workspaces": [
    "api",
    "web",
    "web-app"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@actions/core": "^1.11.1",
    "@types/better-sqlite3": "^7.6.13",
    "concurrently": "^9.2.1",
    "electron": "^38.0.0",
    "wait-on": "^8.0.4"
  }
}
